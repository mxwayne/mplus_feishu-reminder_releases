# M+控制台 0.12.0 安装与使用

## Windows 安装或升级

1. 从 `hub-v0.12.0` 下载 `m-plus-feishu-reminder-0.12.0-windows-x64-setup.exe` 和 `SHA256SUMS.txt`，核对文件哈希。
2. 已有用户先在托盘菜单选择“退出”，等待程序结束；保留当前数据和有效备份。不要删除用户数据来升级。
3. 在 Windows x64 运行安装包。新安装建议使用 `D:\mplus_feishu\app`；已有用户沿用实际安装目录，不要只移动正在使用的程序文件夹。
4. 安装结束后，从桌面“M+控制台”入口启动。

## Mac 安装或升级

1. 根据“关于本机”选择 Apple Silicon（`arm64`）或 Intel（`x64`）安装包，下载对应 DMG 与校验文件。最低要求为 macOS 12，本版在 macOS 15 上构建验证。
2. 升级前正常退出旧版并保留有效数据备份。打开 DMG，把“M+控制台”拖到“应用程序”；也可使用 ZIP 解压得到应用。
3. 从“应用程序”启动。Mac 包只有临时完整性签名，尚无 Apple Developer ID 签名和公证，首次打开可能出现系统安全提示，需由使用者按系统提示处理；不要关闭系统安全保护。
4. Chrome 扩展位于应用包内 `Contents/Resources/collector-extension`；可复制到固定的独立目录加载，后续升级时同步更新。不要从临时挂载的 DMG 加载扩展。
5. Mac 版需手动启动；当前自动注册登录启动项和额外进程恢复任务为 Windows 部署能力。

## 连接 M+ 与飞书

1. 在 Chrome 打开 `chrome://extensions`，启用开发者模式。Windows 加载安装目录下的 `resources/collector-extension`；Mac 加载上文复制到固定位置的扩展目录。如果已有专用扩展目录，更新该目录中的文件后重新加载扩展，不要同时启用两份采集扩展。
2. 在控制台“连接与配置”复制配对码，在扩展中选择对应环境、保存并连接。
3. 在 Chrome 登录 M+ 并打开管理页面，确认控制台显示“自动采集正常”。
4. 在“连接与配置 → 飞书连接设置”保存本机配置，点击“检查当前状态”；在“报表同步”查看最近成功时间。

应用只向 M+ 发起白名单 GET 读取请求。登录信息和飞书凭据不随安装包分发，保存后由当前系统账户加密保护。更新飞书配置前，确认目标报表和通知群。

## 持续运行与备份

- 程序在当前 Windows 账户登录后自动后台启动。保持网络可用、M+ 登录有效，避免电脑自动睡眠；未登录 Windows 时不会采集。
- 关闭控制台窗口仍会后台运行；托盘菜单“退出”会停止程序。
- 在“连接与配置”启用每日加密备份，确认页面显示备份成功。每 24 小时备份一次，保留最近 7 份，失败后重试。
- 通过“打开备份目录”查看备份。不要删除同目录的密钥文件；本机保护的密钥不能直接用于另一台电脑，换机需要使用可移植备份流程。
- 如 M+ 登录过期，请在 Chrome 重新登录。飞书同步失败时，本地采集和保存继续运行，连接恢复后重试。
- 本安装包包含应用自身的恢复逻辑；额外的 Windows 进程恢复计划任务属于部署维护配置，不会由安装包自动安装。

## 换电脑

安装包不会携带或自动迁移旧电脑的数据。迁移时需要恢复控制台数据和业务配置，并在旧电脑扩展的“换电脑：迁移采集进度”中导出进度，在新电脑完成配对后导入。进度文件不含登录凭据或配对码，导入后会刷新 M+ 页面。

一次只运行一个承担同一套报表和日报发送的控制台。换机时先停止旧节点，再启动新节点并核对历史项目完成数、人工抽查记录、飞书同步结果和备份。不要将重新安装当作业务数据已经完成迁移。

## 版本说明

0.12.0 增加制作人待确认清单和跨日过审日期拆分，并改善控制台窄屏布局与状态刷新。

Windows 包没有发行者签名；Mac 包只有临时完整性签名，没有 Developer ID 签名和 Apple 公证。本版不接入旧提醒版自动更新。安装包不含原始工程、测试、开发记录、数据库或 source map；Electron 与 Chrome 所需的压缩 JavaScript 运行代码仍可被分析。每个使用环境的首次真实群推送和长时间运行，仍应按本机实际结果核验。

## 回退与发布信息

每版由相同源码提交构建，使用 `hub-v版本号` 标签。对应 `build-info-平台-架构.json` 记录源码提交、构建任务、文件大小和哈希；`SHA256SUMS.txt` 覆盖全部安装文件和交付说明。

需要回退时，正常退出新版后安装上一版，并使用该版本兼容的数据备份。升级后如数据格式发生变化，不要直接让旧程序读取新数据；先恢复对应备份。保留既有通知群配置与发送记录，避免同时运行两个节点导致重复处理。
